version: "3.7"
services:
  db:
    image: postgres:13.2-alpine
    ports:
      - '8080:8080'
    volumes:
      - ./data:/var/lib/postgresql
    environment:
      - POSTGRES_DB=db
      - POSTGRES_USER=alexnikiforova
      - POSTGRES_PASSWORD=~8qXt~|Y~@?IN